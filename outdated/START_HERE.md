# VR Recommender - 项目启动指南

## 🚀 快速开始

### 方法 1: 一键启动 (推荐)

```bash
# 启动所有服务并在浏览器中查看
./start_project.sh
```

这将自动：
- ✅ 检查并启动 Neo4j 数据库
- ✅ 清理被占用的端口
- ✅ 启动 Flask API
- ✅ 显示实时日志
- ✅ 提供访问地址

### 方法 2: 后台启动

如果你想在后台运行所有服务：

```bash
# 后台启动所有服务
./start_services.sh

# 检查服务状态
./status.sh
```

## 📍 访问地址

启动成功后，你可以访问：

- **Chatbot 界面**: http://localhost:5001
- **API 健康检查**: http://localhost:5001/health
- **Neo4j Web UI**: http://localhost:7474

## 🔧 管理命令

| 命令 | 描述 |
|------|------|
| `./start_project.sh` | 一键启动所有服务 |
| `./start_services.sh` | 后台启动所有服务 |
| `./stop_all.sh` | 停止所有服务 |
| `./restart.sh` | 重启所有服务 |
| `./status.sh` | 查看服务状态 |
| `./check_ports.sh` | 检查端口占用 |

## 📝 使用示例

### 1. 启动项目
```bash
$ ./start_project.sh

╔══════════════════════════════════════════════════════════════════╗
║         VR Recommender Project - 一键启动脚本                     ║
╚══════════════════════════════════════════════════════════════════╝

[INFO] 检查 Neo4j 服务...
[✓] Neo4j 启动成功 (端口 7687)

[INFO] 检查 Flask API 进程...
[✓] 没有发现 Flask API 进程

[INFO] 启动 Flask API on 端口 5000...

╔══════════════════════════════════════════════════════════════════╗
║                    🚀 项目启动成功!                               ║
╚══════════════════════════════════════════════════════════════════╝

📍 可用端点:
   GET  http://localhost:5000/          → Chatbot 界面
   POST http://localhost:5000/chat      → 获取推荐
   GET  http://localhost:5000/health    → 健康检查
```

### 2. 使用 Chatbot
```bash
curl -X POST http://localhost:5001/chat \
  -H 'Content-Type: application/json' \
  -d '{"message": "machine learning for public policy"}'
```

### 3. 停止服务
```bash
$ ./stop_all.sh

═══════════════════════════════════════════════════════════════════
         VR Recommender - 停止所有服务
═══════════════════════════════════════════════════════════════════

[INFO] 停止 Flask API...
[✓] Flask API (PID: 12345) 已停止
[✓] 所有服务已停止！
```

## 🛠️ 故障排除

### 端口被占用

如果遇到 "Address already in use" 错误：

```bash
# 自动清理并重启
./restart.sh
```

### Neo4j 未启动

确保 Neo4j 已安装并添加到 PATH：

```bash
# 检查 Neo4j
neo4j version

# 如果未安装，请访问 https://neo4j.com/download/
```

### 虚拟环境

建议在虚拟环境中运行：

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

## 📦 系统要求

- Python 3.8+
- Neo4j 4.0+
- 端口 5000, 5001, 7474, 7687 可用

## 🎯 测试推荐

启动成功后，尝试这些查询：

1. `"data visualization"`
2. `"Python programming"`
3. `"machine learning for public policy"`
4. `"cybersecurity tools"`

## 📞 获取帮助

```bash
./start_project.sh --help
```

---

**快速命令**: `./start_project.sh` → 访问 http://localhost:5001
